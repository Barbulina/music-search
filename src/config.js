export const DEFAULT_LIMIT = 6;
export const DEFAULT_LIMIT_BY_TYPE = 50;

export const typesToSerach = ["artist", "album", "track"];
export const typesOfLists = {
  artists: "artists",
  albums: "albums",
  tracks: "tracks",
};
export const typesMap = {
  artists: "artist",
  albums: "album",
  tracks: "track",
};

export const DEFAULT_DEBOUNCE_TIME = 200;
