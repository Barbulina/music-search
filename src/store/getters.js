export const getters = {
  token(state) {
    return state.token;
  },
  getList(state) {
    return state.list;
  },
  getTypeList(state) {
    return state.typeList;
  },

  getDetail(state) {
    return state.detail;
  },

  getLastSearch(state) {
    return state.lastSearch;
  },
};
